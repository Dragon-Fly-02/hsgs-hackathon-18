"use strict";
Object.defineProperty(exports, "__esModule", { value: true });

function random(N) {
  return Math.floor(Math.random() * N);
}

const N13 = {
  default(props) {
    
  },

  actions: {
    async move(state, {}) {
      
    }
  },

  isValid(state) {
  // nothing to do here ?
  },

  isEnding(state) {
    
  }
}

exports.default = N13;
